VITE_API_URL=http://localhost:8000

# OSM is the default provider and needs no key. Set VITE_MAP_PROVIDER=maptiler
# only when a MapTiler key is available for the deployment.
VITE_MAP_PROVIDER=osm
VITE_MAPTILER_API_KEY=
